<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="GrownHere is coming soon — a marketplace for local, sustainable food." />
  <title>GrownHere - Coming Soon</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />
  <style>
    :root {
      --background: radial-gradient(circle at top, rgb(130, 173, 92), rgb(133, 97, 50));
      --orange: #B34E0F;
      --dark: #2E2E2E;
      --light: #F9F9F9;
    }

    * {
      box-sizing: border-box;
    }

    /* Accessibility styling */
    .visually-hidden {
      position: absolute;
      width: 1px;
      height: 1px;
      margin: -1px;
      padding: 0;
      overflow: hidden;
      clip: rect(0 0 0 0);
      white-space: nowrap;
      border: 0;
    }

    a:focus,
    button:focus,
    input:focus {
      outline: 2px solid var(--orange);
      outline-offset: 3px;
    }


    html,
    body {
      margin: 0;
      padding: 0;
      font-family: sans-serif;
      background: var(--background);
      color: var(--dark);
      height: 100%;
    }

    body {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem 1rem;
      width: 100%;
      max-width: 600px;
    }

    .logo {
      width: 40vw;
      max-width: 200px;
      min-width: 120px;
      margin-bottom: 2rem;
      opacity: 0.85;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: #ffffff;
    }

    p {
      font-size: 1.2rem;
      max-width: 500px;
      font-weight: 700;
      margin: 0 auto 2rem auto;
      color: #eeffee;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
      align-items: center;
      margin-top: 1rem;
      width: 100%;
    }

    input[type="email"],
    input[type="text"] {
      padding: 0.8rem;
      width: 100%;
      max-width: 320px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 1rem;
    }

    button {
      padding: 0.8rem 1.2rem;
      background-color: var(--orange);
      width: 55%;
      color: rgb(255, 255, 255);
      border: none;
      border-radius: 4px;
      font-size: 1.05rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s ease-in-out;
    }

    button:hover {
      background-color: #973F0D;
    }

    .social {
      margin-top: 1.5rem;
      font-size: 1.25rem;
    }

    .social a {
      color: #ffffff;
      text-decoration: none;
      margin: 0 0.5rem;
      transition: color 0.2s ease;
    }

    .social a:hover {
      color: var(--orange);
    }


    footer {
      font-size: 0.85rem;
      color: #ccc;
      margin-top: 2rem;
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 1.75rem;
        margin-bottom: 15px;
      }

      p {
        font-size: 0.95rem;
      }
    }
  </style>
</head>

<body>
  <main role="main">
    <div class="container">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 250 60" fill="none" class="logo" role="img"
        aria-label="GrownHere Logo">
        <text x="5" y="45" font-family="Vollkorn, serif" font-size="40" fill="#eee" font-weight="bold">
          GrownHere
        </text>
      </svg>


      <h1>We're planting the seeds...</h1>
      <p>GrownHere is launching soon — a UK-grown marketplace for local people, producers, and farm shops.</p>

      <form action="https://formspree.io/f/mqaqwqkk" method="POST">
        <label for="name" class="visually-hidden">Name</label>
        <input type="text" id="name" name="name" placeholder="Enter your name..." required />
        <label for="email" class="visually-hidden">Email</label>
        <input type="email" id="name" name="email" placeholder="Enter your email..." required />
        <button type="submit">Notify Me!</button>
      </form>

      <div class="social">
        Follow us:
        <a href="https://twitter.com/uk_grown" target="_blank" aria-label="Twitter / X">
          <i class="fab fa-twitter"></i> / <i class="fab fa-x"></i>
        </a>
        <a href="https://facebook.com/grownhereuk" target="_blank" aria-label="Facebook">
          <i class="fab fa-facebook"></i>
        </a>
      </div>

      <footer>
        &copy; {{ ansible_date_time.year }} GrownHere Ltd. All rights reserved.<br />
        Built with ❤️ on the <a href="https://openfoodnetwork.org" target="_blank" style="color:#ccc;">Open Food
          Network</a>.
      </footer>
    </div>
  </main>
</body>

</html>